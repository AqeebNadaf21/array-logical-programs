// 1. Javascript program to print the number of an element present in array.
function numOfElements(arr) {
    console.log('Number of elements in array:', arr.length);
}
let array = [1, 2, 3, 4, 5, 6];
numOfElements(array)

